# Development Dockerfile for Spring Boot with watch mode
FROM eclipse-temurin:21-jdk-alpine

# Install dependencies for watch mode
RUN apk add --no-cache curl

WORKDIR /app

# Copy Maven wrapper and dependencies
COPY mvnw .
COPY .mvn .mvn
COPY pom.xml .

# Make Maven wrapper executable
RUN chmod +x ./mvnw

# Download dependencies (cached layer)
RUN ./mvnw dependency:go-offline -B

# Copy source code (will be overridden by watch volumes)
COPY src src

EXPOSE 8080

# Development command with Spring Boot DevTools (skip tests)
CMD ["./mvnw", "compile", "spring-boot:run", "-Dspring-boot.run.profiles=dev", "-Dmaven.test.skip=true"]
